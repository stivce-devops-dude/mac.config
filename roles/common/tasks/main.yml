---
- name: Create home directories
  ansible.builtin.file:
    path: "{{ lookup('env', 'HOME') }}/{{ item }}"
    state: directory
    mode: "0755"
  loop: "{{ home_directories }}"
  tags:
    - directories
